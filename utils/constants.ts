export const STAY_TUNNED = "Stay always tuned with planting trends";
export const CURRENT_TEMPERATURE = (temp: number) =>
  `Current temperature is: ${temp} °C`;
export const TIME_CREATED_AND_READ = (createdAt: string, timeRead: string) =>
  `${createdAt} · ${timeRead}`;
